/**
 * Moved to the repo-root lib/ — see lib/README.md for why.
 *
 * This file stays as a redirect so existing `./maps` imports keep working. It
 * re-exports; it does not redefine. New code should import from
 * '@asot/lib' directly.
 */

export {
    AWARD_TO_CITATION,
    QUAL_TO_BADGE,
    SECTION_TO_BADGE,
    DEFAULT_BADGE,
} from '@asot/lib'
