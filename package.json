{
  "name": "asot-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/bot"
  ],
  "scripts": {
    "install:all": "npm install && npm --prefix apps/web install",
    "menu": "node scripts/menu.mjs"
  },
  "dependencies": {
    "@clack/prompts": "^1.7.0"
  }
}
